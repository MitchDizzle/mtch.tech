---
layout: layouts/shell.njk
permalink: /play/
title: Loading
description: Mitchtopia - interactive mode.
---
{% from "components/panel.njk" import panel %}

{# The game route. Options moved to the landing page as an in-place panel
   swap, since they are a confirmation step rather than a destination.

   This page is where asset loading and the canvas will live. For now it is a
   placeholder so the flow terminates somewhere honest instead of 404ing. #}

<main class="landing">
  {% call panel({ variant: "prompt", title: "Not built yet", labelledBy: "play-title" }) %}
    <p class="muted">
      Interactive mode is still being made. The street, the storefronts, and
      the first minigame come before anything is playable here.
    </p>
    <p class="muted small">
      Your options were saved - they will be waiting when there is something
      to load.
    </p>
    <div class="panel__actions">
      <a class="btn" href="/" data-panel-exit>Back to Mitchtopia</a>
      <a class="btn" href="/site/" data-panel-exit>Simplified site</a>
    </div>
  {% endcall %}
</main>
